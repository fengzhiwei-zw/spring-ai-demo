spring:
  application:
    name: spring-ai-demo
  ai:
    openai:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: ${DASHSCOPE_API_KEY}
      chat:
        options:
          model: qwen3.8-27b
    chat:
      memory:
        redis:
          host: localhost
          port: 6379


